<%= render 'navbar', shoutouts: @shoutouts %>

<div class="container">
  <h1 class="text-center">Admin</h1>
  <% @shoutouts.each do |s| %>
    <%= render 'shoutouts/shoutout', shoutout: s, winner: s.cheers.size == @winning_count, hide_votes: false %>
  <% end %>
</div>
